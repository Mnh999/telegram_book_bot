services:
  - type: worker
    name: dance-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dance_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
